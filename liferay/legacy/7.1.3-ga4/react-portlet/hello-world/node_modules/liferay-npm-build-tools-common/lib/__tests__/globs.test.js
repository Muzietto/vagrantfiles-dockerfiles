'use strict';

var _globs = require('../globs');

it('negate works', function () {
	var globs = ['**/*', '!index.js'];

	expect((0, _globs.negate)(globs)).toEqual(['!**/*', 'index.js']);
});

it('prefix works', function () {
	var globs = ['**/*', '!index.js'];

	expect((0, _globs.prefix)('dir/', globs)).toEqual(['dir/**/*', '!dir/index.js']);
});
//# sourceMappingURL=globs.test.js.map